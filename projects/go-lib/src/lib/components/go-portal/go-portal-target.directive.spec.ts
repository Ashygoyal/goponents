import { GoPortalTargetDirective } from './go-portal-target.directive';

describe('GoPortalTargetDirective', () => {
  it('should create an instance', () => {
    const directive = new GoPortalTargetDirective();
    expect(directive).toBeTruthy();
  });
});
